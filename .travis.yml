language: rust
rust:
    - stable
    - beta
    - nightly

matrix:
    allow_failures:
        - rust: nightly

script:
    - cargo build --all-features
    - cargo test --all-features

before_install:
    - sudo apt-get install libcap2
